# B1-1 | 휴대전화로 마지막 검사와 제출하기

코드 업로드와 GitHub Pages 배포는 완료했습니다. 2026-09-20 Pages 빌드·배포 성공 기록을 확인했으며 사용자가 휴대전화에서 사이트가 열린다고 확인했습니다. PC·GitHub 모바일 앱 설치는 필요하지 않습니다.

- 저장소: https://github.com/usernameffas/codyssey-b1-1
- 실제 웹사이트: https://usernameffas.github.io/codyssey-b1-1/

## 1. 실제 웹사이트 기능 검사

Chrome 또는 삼성 인터넷에서 **실제 웹사이트 주소**를 열고 아래를 순서대로 확인합니다.

1. 우측 위 메뉴(☰) 열기 → `프로젝트` 탭 → 해당 영역으로 이동하고 메뉴가 닫히는지 확인.
2. 위쪽 테마 버튼 누르기 → 화면이 어두워지는지 확인 → 새로고침 후에도 다크 모드인지 확인.
3. `GitHub 프로젝트` 영역까지 내려가기 → E1 등 공개 저장소 카드가 표시되는지 확인. 로딩이 계속되거나 오류가 뜨면 해당 화면을 캡처.
4. `문의`에서 빈 채로 `입력 확인하기` 누르기 → 오류 메시지 확인. 잘못된 이메일도 검증해 보기. 정상 입력 뒤에는 **실제 전송되지 않는다**는 안내가 나오는지 확인.
5. 아래로 내렸을 때 ↑ 버튼을 누르면 최상단으로 이동하는지 확인.

실제 개인정보는 문의 양식 테스트에 넣을 필요가 없습니다. 이메일 예시는 `test@example.com`을 사용하세요.

## 2. 제출용 화면 캡처 3장

실제 웹사이트를 연 상태에서 삼성 휴대전화의 **전원 + 음량 줄이기**를 동시에 눌러 촬영합니다. 저장 파일은 갤러리의 스크린샷 또는 내 파일의 Pictures/Screenshots에서 확인할 수 있습니다.

1. `mobile.png`: 일반 모바일 화면을 밝은 모드에서 촬영.
2. `dark.png`: 테마 버튼으로 다크 모드를 켠 화면을 촬영.
3. `desktop.png`: 가능하면 PC 브라우저의 넓은 화면에서 촬영. 휴대전화만 사용한다면 Chrome 메뉴 `⋮` → `데스크톱 사이트`를 켜 넓은 레이아웃이 실제로 표시되는지 확인하고 촬영합니다. 이 경우 캡처가 휴대전화의 데스크톱 사이트 보기라는 점을 분명히 합니다. 일반 모바일 화면과 동일하게 보이면 캡처를 데스크톱 검증으로 간주하지 않습니다.

가능하면 캡처 원본 3장을 이 대화에 첨부해 주세요. 개발 중 만든 `qa-*-mock.png`는 **모의 데이터 화면**이므로 게시 사이트의 캡처로 제출하지 않습니다.

## 3. 코디세이 제출

과제 제출란에 위 **저장소 URL**과 **웹사이트 URL**을 입력하고 데스크톱·모바일·다크 모드 스크린샷 3장을 첨부합니다. 제출 버튼을 누른 뒤 완료 표시가 나타나는지 확인합니다. 캡처를 GitHub 저장소에 올려야 한다면 `docs/screenshots/` 폴더에 `desktop.png`, `mobile.png`, `dark.png` 이름으로 업로드하고 README를 연결합니다.

다른 계정의 비밀번호·인증 코드·GitHub 토큰을 코드나 채팅에 입력하지 마세요.
